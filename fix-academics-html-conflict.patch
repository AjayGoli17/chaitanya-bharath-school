diff --git a/academics.html b/academics.html
index faaa1e8..037083b 100644
--- a/academics.html
+++ b/academics.html
@@ -46,7 +46,7 @@
         <p>We provide a balanced learning experience that combines academics, life skills, creativity, technology, and overall personality development.</p>
     </section>
 
-    <section class="container philosophy-grid stagger-children">
+    <section class="container philosophy-grid">
         <div class="card cell-1">
             <div class="icon-wrap">
                 <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#9A7E15" stroke-width="2">
@@ -300,26 +300,5 @@
 
 
     <script src="js/academics.js"></script>
-    <script>
-    // Fallback scroll-trigger for the philosophy grid, in case
-    // main.js doesn't already watch .stagger-children elements.
-    // Safe to keep even if main.js has its own observer — it
-    // only ever adds the 'visible' class, never removes it.
-    (function () {
-        var targets = document.querySelectorAll('.stagger-children:not(.visible)');
-        if (!targets.length || !('IntersectionObserver' in window)) return;
-
-        var observer = new IntersectionObserver(function (entries) {
-            entries.forEach(function (entry) {
-                if (entry.isIntersecting) {
-                    entry.target.classList.add('visible');
-                    observer.unobserve(entry.target);
-                }
-            });
-        }, { threshold: 0.2, rootMargin: '0px 0px -60px 0px' });
-
-        targets.forEach(function (el) { observer.observe(el); });
-    })();
-    </script>
 </body>
 </html> 
\ No newline at end of file
